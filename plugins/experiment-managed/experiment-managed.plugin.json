{
  "pluginContract": 3,
  "name": "experiment-managed",
  "version": "0.1.0",
  "spawns": false,
  "client": true,
  "instructions": null,
  "tools": [
    {
      "name": "experiment_submit",
      "description": "Submit a goal into this session, to exercise the plugin client.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "goal": { "type": "string", "description": "What to ask the agent to do, as a user would type it." },
          "want_result": { "type": "boolean", "description": "Wait for the turn and return its final answer, rather than only confirming it was accepted." }
        },
        "required": ["goal"]
      },
      "gated": false
    }
  ]
}
